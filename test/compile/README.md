# test-compile

Use `gcc` to test if the driver code compiles.

Run test:
 * `chmod +x <test-name>.sh`
 * `./<test-name>.sh`


**test-compile-static.sh**

Test a static compile of the driver


**test-compile-static.sh**

Test a compile of the driver after install
